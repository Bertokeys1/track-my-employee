const connection = require('connection');

class Directory {
    constructor(connection){
        this.connection = connection
    }
//create the query that is needed. example findAllDepartments make usre to use promise from mysql2
}

module.exports =  new Directory(connection)